# dependências
node_modules/

# builds de caso (output específico de marca — vive fora da skill viva)
builds/

# OS / editor
.DS_Store
*.log

# li-cli (sessão/credenciais ficam no home do usuário, nunca no repo — guard extra)
.li/
*.session
